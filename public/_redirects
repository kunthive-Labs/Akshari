# Existing files (/, /terms.html, /privacy.html, /assets/*) and the /api/*
# Pages Function are served first; only unmatched paths fall through to the
# app shell. The app has no client-side router, so this is just a safety net.
/*    /index.html   200
